environments:
    development:
        adapter: sqlite
        name: ./db/phpsqlite
        suffix: ".db"    # Defaults to ".sqlite3"
    testing:
        adapter: sqlite
        memory: true     # Setting memory to *any* value overrides name